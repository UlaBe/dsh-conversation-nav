/**
 * @ulabe/dsh-conversation-nav — host half.
 *
 * Flat-list variant: no topic tree, no system-prompt injection. Provides no
 * host-side behavior; the browser half ships via exports["./client"].
 */
export function apply() {}
